const user = {name : string; age: number; isAdmin: boolean} = {
    name: 'sami',
    age: 25,
    isAdmin: true
};

const {age, isAdmin: adminStatus} = user;

const userInfo = ['syed sami', 25];
const [userName, userAge ] = ['syed sami', 25];

